<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white">

    <!-- Unified top bar: notebook / page nav / tools -->
    <include layout="@layout/navbar" />

    <!-- Border line: marks the bottom edge of the bar / top of the canvas -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/black" />

    <!-- Canvas container: the DrawView fills it; the text-box editor (an EditText) is added on
         top, positioned over the box being edited. A FrameLayout so children can overlap. -->
    <FrameLayout
        android:id="@+id/canvas_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <com.forestnote.app.notes.DrawView
            android:id="@+id/draw_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white" />
    </FrameLayout>

</LinearLayout>
