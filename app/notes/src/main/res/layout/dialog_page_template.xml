<?xml version="1.0" encoding="utf-8"?>
<!-- Per-page template override picker (library-and-tools B4). "Use default" writes
     NULL (inherit the global default); Blank/Dot/Ruled/Grid write an explicit
     override. Pitch sub-radio (built in code from SettingsFormLogic presets) shows
     only for a drawn template. -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="12dp"
        android:paddingBottom="8dp">

        <TextView style="@style/SettingsLabel" android:text="Template for this page" />

        <RadioGroup
            android:id="@+id/rg_pt_template"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <RadioButton android:id="@+id/rb_pt_default" style="@style/SettingsRadio" android:text="Use default" />
            <RadioButton android:id="@+id/rb_pt_blank" style="@style/SettingsRadio" android:text="Blank" />
            <RadioButton android:id="@+id/rb_pt_dot" style="@style/SettingsRadio" android:text="Dot" />
            <RadioButton android:id="@+id/rb_pt_ruled" style="@style/SettingsRadio" android:text="Ruled" />
            <RadioButton android:id="@+id/rb_pt_grid" style="@style/SettingsRadio" android:text="Grid" />
        </RadioGroup>

        <LinearLayout
            android:id="@+id/row_pt_pitch"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView style="@style/SettingsLabel" android:text="Pitch" />

            <RadioGroup
                android:id="@+id/rg_pt_pitch"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>
