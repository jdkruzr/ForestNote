# Notes App (app:notes)

Last verified: 2026-05-29 (caldav-todo-button)

## Purpose
Main application module that wires together ink rendering, storage, and user interaction into a single-activity note-taking experience optimized for e-ink. Includes a nav bar for moving between pages, plus a full-screen Library overlay for browsing folders and notebooks as a card grid.

## Contracts
- **Exposes**: `MainActivity` (entry point), `DrawView` (drawing surface), `ToolBar` (tool selection), `PageNavigationLogic` (pure page index/bounds/label math), `ToolSelectionLogic` (pure tool/variant state), `LassoSelectionLogic` (pure selection geometry), `Clipboard`/`InProcessClipboard`, `SelectionMenuView`, `SettingsView` (full-screen Settings overlay), `SettingsFormLogic` (pure pitch-radio rules), `LibraryView` (full-screen Library overlay; `Callbacks.onOpenSearch` + public `navigateToFolder(folderId?)`), `RecycleBinView` (full-screen Recycle Bin overlay), `LibraryAdapter`/`LibraryItem` (sealed `Folder`/`Notebook`), `BreadcrumbView`, `LaunchLogic`/`BreadcrumbLogic`/`NotebookNameParser`/`RelativeTime`/`ThumbnailCacheLogic` (pure helpers), `ThumbnailRenderer`/`ThumbnailCache`/`ThumbnailLoader`, `TextBoxEditOverlay` (full-screen edit modal — subsumes the legacy in-canvas `TextBoxEditor` + the standalone Options dialog), `TextBoxEditOverlayLogic` (pure commit/cancel decisions), `TextBoxMenuView` (Edit/Options/Delete pill — both Edit and Options open the overlay, differing only in initial focus), `FontCatalog` (/system/fonts enumeration + fallback), `search.SearchDialog` (modal AlertDialog over the Library) + `search.SearchResultsAdapter`, `ocr.OcrTextDialog` (editor modal showing server-recognized text with a forward-compatible "Device recognized" stub entry in the source dropdown), `caldav.*` (lasso-recognize → CalDAV VTODO flow with offline outbox/drainer — see Key Decisions: `VTodoBuilder`/`VTodoInput`/`VTodoDue`, `CalDavClient` + `OkHttpCalDavClient` + `CalDavCredentials` + `CalDavResult`, `SecureCredentialsStore` + `EncryptedPrefsCredentialsBackend` + `SyncCredentials`, `CalDavTaskSheetLogic` + `DueChoice`, `CalDavTaskSheet`, `CalDavOutboxStore` (drainer-facing interface; bridged to `NotebookStore` single-writer thread), `CalDavOutboxLogic` + sealed `DrainOutcome` (Success / RetryLater / DeadLetter — pure retry policy), `CalDavOutboxDrainer` + nested `DrainerStatus`/`Phase` + sealed `TryOutcome` (Sent / Queued), `NetworkAvailabilityMonitor`)
- **Guarantees**: Strokes auto-save on pen-up. Strokes restore on app launch (non-blocking async load), reopening the last-active notebook+page; if there is no resumable notebook (blank/empty active id or empty library, per `LaunchLogic`) the app opens into the Library instead. Backend lifecycle managed across pause/resume. Finger input rejected (stylus/eraser only). Page/notebook switching commits the current stroke (via clearAll) and refreshes the canvas to avoid e-ink ghosting. Lasso selects strokes by centroid; Cut/Copy/Delete via a floating pill + in-process clipboard; Paste is tap-to-place (arm, then tap the canvas); a closed selection can be dragged to move it in place. The Library lists folders-then-notebooks inside the current folder (null = root) as a 4-column card grid; tap a notebook to open it, tap a folder to descend, breadcrumb/back to walk up.
- **Expects**: `core:ink` for backend and stroke types, `core:format` for persistence.

## Dependencies
- **Uses**: `core:ink` (InkBackend, BackendDetector, PageTransform, Stroke, Tool, StrokeGeometry, PressureCurve), `core:format` (NotebookRepository + NotebookMeta/PageMeta/FolderMeta/NotebookCard/FolderCard/FolderPathLogic, owned exclusively by `NotebookStore`), `core:sync` (SyncEngine/SyncConfig/SyncStatus + HttpUrlTransport, driven by `SyncController`), `androidx.recyclerview` (Library card grid only), kotlinx-coroutines (sync controller scope), `com.squareup.okhttp3:okhttp` (CalDAV PUTs only — `caldav.OkHttpCalDavClient`), `androidx.security:security-crypto` (EncryptedSharedPreferences backing `caldav.SecureCredentialsStore`)
- **Used by**: Nothing (top-level app module)
- **Boundary**: Should not contain domain logic; delegates to core modules

## Key Decisions
- Activity (not Fragment/Compose): minimal overhead for single-screen e-ink app. Secondary screens (Settings, Library) are full-screen overlay Views added to `android.R.id.content`, not new Activities — keeps the single `NotebookStore` (single-writer DB invariant) and avoids fragments. The Library is reached by tapping the fixed-width Library grid icon in the nav bar (a fixed icon, NOT the variable-width notebook name, so different name lengths never reflow the bar; the old `showNotebookPicker` dialog is REMOVED); its own folder/notebook/create dialogs are driven by `MainActivity` via `LibraryView.Callbacks`
- **Toolbar caption convention (both the editor toolbar and the Library header):** every action is a vertical icon-over-caption cell where the cell is the clickable hitbox and the inner `ImageButton` is non-clickable (taps fall through). A trailing **`▾`** on the caption means "opens a chooser/dialog" (editor: `Fountain ▾`/`Stroke ▾`/`Template ▾`; Library: `Folder ▾`/`Notebook ▾`); a plain caption is a direct action (`Lasso`/`Clear`/`Refresh`/`Settings`/`Up`). Captions are static strings (fixed widths → no reflow). The editor toolbar is 30dp; the Library header is 44dp (icon+caption + breadcrumb needs the extra room)
- RecyclerView is used only by the Library card grid (`androidx.recyclerview` — the app's first and only RecyclerView). Thumbnails render off-thread (`ThumbnailLoader`) into an LRU `ThumbnailCache`; eviction/sizing math is pure in `ThumbnailCacheLogic`
- DrawView owns offscreen bitmap: all strokes rendered to bitmap, blitted to screen in onDraw. A single `composeStaticBitmap()` defines page layering — template → BOTTOM-band text boxes → ink → TOP-band text boxes — and every rebuild path funnels through it (onSizeChanged/mergeLoadedStrokes/mergeLoadedTextBoxes/clearAll/redrawBitmap); `setTemplate(effectiveTemplate, pitchMm)` repaints on change
- **Text boxes (Tool.Text):** z-ordered text elements rendered with `StaticLayout`, clipped to the box (overflow retained in the model, not drawn). Drag-to-draw places a box; the full-screen `TextBoxEditOverlay` (attached to `android.R.id.content`, opaque white) edits it — text + font + size + weight + border + z-band all in one screen. The overlay obscures the canvas, so the soft keyboard's pan/resize no longer ghosts on the e-ink panel ([[viwoods-adjustnothing-anr]] proved manifest tweaks are a dead end on Viwoods). A freshly drag-drawn box is held as `pendingNewBox` (not yet in `textBoxes`, not persisted) — Cancel discards it cleanly with no DB write. `transformingBoxId` boxes are skipped in the static bitmap (shown live in `onDraw`). The overlay's `commitIfShowing()` fires from tool/page/notebook switch, library open, and onPause; back-button calls `requestCancel()`. On Done, `DrawView.commitOverlayBox` recomputes the rendered height via `measureTextBoxHeightPx` (same StaticLayout shape as `drawTextBox`) and persists. Empty text on Done routes to discard (pending-new gone, existing-box soft-deleted). Tap a box to select (corner handles + `TextBoxMenuView` pill); drag interior = move, drag handle = resize (reflows live). Both pill buttons (Edit, Options) open the same overlay — Edit focuses the EditText so the keyboard pops, Options leaves it defocused so the style strip is the first interaction. Font/size catalog is `FontCatalog` (off-thread `/system/fonts` scan; resolves a stored basename to a Typeface with a default fallback so a synced box whose font is absent still renders). Active style persists in `Settings.textFontName`/`textFontSizeV`. Text-box SYNC is gated off client-side (see core:format) — boxes persist + render locally now. Template pitch uses true panel PPI (293 on the Mini, set in MainActivity by isEInk), NOT densityDpi/xdpi
- Hardware eraser (TOOL_TYPE_ERASER) always erases regardless of toolbar selection
- 900ms postDelayed invalidate after pen-up: triggers quality e-ink refresh without excessive redraws
- All persistence flows through `NotebookStore` (single background thread); DrawView/MainActivity never touch `NotebookRepository` or the DB directly. Save is fire-and-forget; erase reconcile + load run off-thread and post results back to the main thread
- **CalDAV tasks + secret storage:** sync credentials and CalDAV credentials live in `EncryptedSharedPreferences` (`forestnote_secrets`, AES-256 in Android Keystore), owned by `caldav.SecureCredentialsStore` over a `KeyValueBackend` interface (production: `EncryptedPrefsCredentialsBackend`). `Settings.syncUsername`/`syncPassword` data-class fields are kept for backward compatibility (no SQLDelight schema change) but are **drained to empty strings** by a one-shot migration in `MainActivity.onCreate` (`SecureCredentialsStore.migrateSyncCredsFromSettings → MigrationResult.Migrated`); readers MUST go through ESP afterwards. `SyncController.resolveCredentials` reads ESP first, falls back to Settings so a pre-migration boot still syncs. CalDAV creds (collectionUrl + username + password) are ESP-only — they were never in Settings
- **Lasso pill routing (Recognize vs. To-do, split by intent):** the two existing lasso-selection buttons now do their own things. **Recognize** = "convert handwriting to a text box on the page" — always runs ML Kit → `MainActivity.insertRecognizedAsTextBox` → `DrawView.prepareRecognizedTextBox` → `TextBoxEditOverlay`, restoring the 219f2dd behaviour for everyone, CalDAV-configured or not. **To-do** = "send handwriting as a CalDAV VTODO" — checks `secureCreds.caldavCreds()`; when configured, runs ML Kit → straight into `CalDavTaskSheet` (full-screen modal, peer of `SettingsView`; no intermediate pill — the sheet's editable SUMMARY is the confirmation surface) → on Send: `NotebookStore.enqueueCalDavTask` (writes a row to `caldav_outbox`) → `caldavDrainer.tryImmediately(uid, OPTIMISTIC_SEND_TIMEOUT_MS=500)` races the periodic drainer for an optimistic toast: `TryOutcome.Sent` → "Task created", `TryOutcome.Queued` → "Task queued". When To-do is tapped without CalDAV configured, the long-standing `SelectionActionLogic.todo` placeholder dialog tells the user where to go. Both lasso buttons call `selectionMenu.dismiss()` immediately on tap so the "N selected …" pill never lingers under the overlays. Routing decision lives at the call site (`onRecognize` / `onTodo`) via an `onText: (text, screenBounds) -> Unit` callback threaded through `showRecognizeFlow`/`runRecognize`. Hand-built iCalendar VTODO (no dav4jvm/ical4j) per defensive-coding ethos; V1 is single-collection PUT-only, no PROPFIND discovery / ETag tracking / scheduling. `onBackPressed` adds a branch for `caldavTaskSheet` ahead of the existing overlays
- **CalDAV offline outbox + drainer (this branch):** failed PUTs never lose tasks. UI enqueues into `caldav_outbox` (LOCAL-ONLY SQLite table, see core:format Gotchas); the body is FROZEN at enqueue while credentials/URL are resolved FRESH on every drain attempt from `SecureCredentialsStore` — that asymmetry is what lets a user fix their password and have stuck rows recover. `CalDavOutboxDrainer` mirrors `SyncController`'s shape on purpose (resume/pause/Mutex-serialised pass/30-min periodic timer, `StateFlow<DrainerStatus>`); anyone touching one should consider the other. Retry policy lives in pure `CalDavOutboxLogic`: 2xx → Success; **412 → Success** (idempotent create-retry pattern — RFC 7231 + Outlook CalDAV Synchronizer / Sabre DAV); 5xx / 429 / `TransportError` → `RetryLater` with exponential backoff 5s → 10s → 20s … capped at 1h (no jitter — single-user queue); other 4xx → `DeadLetter`. `tryImmediately` is the Send-side optimistic toast race against the drainer. `NetworkAvailabilityMonitor` (defensive `ConnectivityManager.registerDefaultNetworkCallback` wrapper, lifecycle-bound to `onStart`/`onStop`) kicks `drainNow()` when WiFi comes back — addresses the Delta-WiFi failure mode. **No WorkManager** — the AiPaper is foreground-driven; SQLite + lifecycle hooks + `NetworkCallback` cover the actual failure mode. **No "X tasks failed" snackbar** — failures surface only in Settings → "Queued tasks" (by deliberate scope choice during brainstorming)

## Invariants
- All coordinates in DrawView: virtual for storage, screen for bitmap rendering
- WritingBufferQueue released in onPause, reacquired in onResume (shared device resource)
- clearAll() only clears in-memory state; database clearing is caller's responsibility (via `store.clear`)
- Tool selection is pure logic in ToolSelectionLogic (testable without Android)
- `DrawView.mergeStrokes` is a pure companion function: loaded ink first, then session ink, deduped by ULID — so the async load never clobbers strokes drawn during the load gap
- `MainActivity.onDestroy` calls `store.shutdown()`, which drains pending saves before closing the driver
- Page/notebook index math is pure in `PageNavigationLogic` (testable without Android); `MainActivity` drives all switching/CRUD through `NotebookStore` and never touches `NotebookRepository` directly
- `goToPage`/`goToNotebook`/`reloadCurrentPage` share one idiom: `clearAll()` → `store.switch*`/`load` → `mergeLoadedStrokes` + `fullRefresh` + refresh indicator/label

## Key Files
- `MainActivity.kt` - Lifecycle management, backend + NotebookStore wiring, nav-bar wiring (page picker; notebook label opens the Library), Library callback wiring + folder/notebook dialogs, launch-into-Library decision (`LaunchLogic`), crash handler. onCreate order: fileLogger → `SecureCredentialsStore(EncryptedPrefsCredentialsBackend(this))` → `OkHttpCalDavClient` → `NotebookStore.create(this, secureCreds)` (calDavClient parameter is GONE — the store no longer owns the client) → `SyncController(..., secureCreds = secureCreds)` → `CalDavOutboxDrainer(outboxStore = store.calDavOutboxStore(), client = caldavClient, secureCreds, scope = syncScope, …)` → `NetworkAvailabilityMonitor(this)`. The `loadSettings` block runs the one-shot ESP migration (`migrateSyncCredsFromSettings` → on `Migrated`, writes back a cleared `SettingsCredsView` via `store.updateSettings`). Hosts `caldavTaskSheet: CalDavTaskSheet` + `caldavDrainer` + `caldavNetworkMonitor`. Lasso-pill `onRecognize` calls `selectionMenu.dismiss()` then `showRecognizeFlow(strokes, bounds) { text, b -> insertRecognizedAsTextBox(text, b) }`; lasso-pill `onTodo` calls `selectionMenu.dismiss()` then, if `secureCreds.caldavCreds() != null`, `showRecognizeFlow(strokes, bounds) { text, _ -> openCalDavTaskSheet(text) }` (else the legacy `SelectionActionLogic.todo` placeholder dialog). `showRecognizeFlow` / `runRecognize` take an `onText: (text, screenBounds) -> Unit` callback so the "what-to-do-with-the-recognized-text" decision lives at the call site. `openCalDavTaskSheet(prefill).onSend` builds the `VTodoInput`, calls `store.enqueueCalDavTask`, then `caldavDrainer.tryImmediately(uid, OPTIMISTIC_SEND_TIMEOUT_MS=500)` chooses between "Task created" (Sent) and "Task queued" (Queued) toasts — the old `handleCalDavResult` AlertDialog popup is GONE. Lifecycle: `onStart` calls `caldavNetworkMonitor.start { caldavDrainer.drainNow() }`, `onStop` calls `caldavNetworkMonitor.stop()`, `onResume` adds `caldavDrainer.resume()`, `onPause` adds `caldavDrainer.pause()`, `onDestroy` adds `caldavDrainer.shutdown()` before the existing teardown. `SettingsView.show(...)` gained a `caldavDrainer` argument so the Queued-tasks section can collect `drainer.status`
- `NotebookStore.kt` - Single background-thread owner of NotebookRepository; async load/save/erase/clear, text-box load/save/delete (`loadTextBoxes`/`saveTextBox`/`deleteTextBox`), notebook/page list-switch-CRUD (`switchNotebook`, `switchPage`, and `switchNotebookToPage(notebookId, pageId, onLoaded)` for Library-search page-level routing), folder CRUD + Library card reads (`listNotebookCardsInFolder`/`listFolderCardsForParent`/`folderPath`) + thumbnail source wrappers, library `search(query, onResult)` aggregator (off-thread wrapper over `NotebookRepository.search`), settings load/update + per-page setPageTemplate, drain-on-shutdown. Sync (Phase 5): an `onDb` executor→coroutine bridge exposes a `SyncLocalStore` for the engine + suspend join bridges (`syncMintSiteId`/`syncBackfillOutbox`/`syncIsPristineBootstrap`/`syncDiscardBootstrapNotebook`/`syncNotebookIds`/`syncCurrentNotebookId`/`syncSettings`) — all still on the single DB writer thread. CalDAV (offline-outbox era): constructor takes optional `secureCredentials: SecureCredentialsStore? = null` (factory `create(context, secureCredentials?)`); the old `calDavClient` parameter is GONE — the store no longer owns the client, the drainer does. Outbox methods all run off the single-writer thread: `enqueueCalDavTask(input: VTodoInput, onDone)` builds the body via `VTodoBuilder.build` and inserts the row, `listCalDavOutbox(onResult)` reads, `deleteCalDavOutboxEntry(id, onDone)` / `retryCalDavOutboxEntry(id, onDone)` are the Settings-row user actions. `calDavOutboxStore(): CalDavOutboxStore` (the drainer-facing bridge, mirrors `syncLocalStore()`) exposes suspend `nextDrainable(now)` / `findById(id)` / `markAttempted(...)` / `markDeadLettered(id, lastError)` / `delete(id)` / `counts(): Pair<Int,Int>` all routed back onto the executor
- `Logging.kt` - On-device file logging for the SSH/Termux debug loop (the locked device gives Termux no logcat access to the app). `LogFormatter` (pure, UTC grep-friendly lines) + `FileLogger` (appends gated lines to `/sdcard/Download/forestnote.log`, never throws; dir injected for tests). Two rotation policies cooperate: (a) **daily rotation** — on the first write of each new UTC day the current file is renamed to `forestnote.log.YYYY-MM-DD` and a fresh file starts; dated archives older than `retentionDays` (default 7) are swept on rotation; (b) **same-day size cap** — within a single day, overflow above `maxBytes` (default 512 KB) spills to a single `forestnote.log.1` slot (not subject to retention sweep). Gated by `Settings.debugLogging` (default off, toggled in Settings "Debug" section). MainActivity builds it (primary `/sdcard/Download`, fallback app-private `filesDir/logs`) and feeds its `log` lambda into `SyncController` → `SyncEngine`/`HttpUrlTransport`, plus the lasso-recognize flow (`Recognize`/`Box`/`Sel` tags) for selection-chain diagnostics
- `SyncController.kt` - The UltraBridge sync trigger layer (Imperative Shell): builds a `SyncEngine`+`HttpUrlTransport` from persisted Settings each session (so credential/URL edits take effect), serializes sessions via a `Mutex`, retries `Retryable` with `SyncBackoff`, runs the pull-first join handshake (`enableAndJoin`: mint → pull → discard untouched bootstrap if the server delivered content → backfill → push), drives the periodic timer, and exposes `StateFlow<SyncStatus>`. `resume()`/`pause()` are the lifecycle hooks (MainActivity onResume/onPause + Settings close); `syncNow()` is the manual button. Sync stays dormant when no server URL is configured (`SyncConfig.from` → null). `resume()` keeps running the full (idempotent) `enableAndJoin` until the persisted `sync_state.joined` flag is set — so a first attempt that failed mid-handshake (no network, wrong password) still uploads pre-sync content on a later resume instead of degrading to a plain `runSession`. Constructor gained `secureCreds: SecureCredentialsStore? = null`; private `resolveCredentials(s: Settings)` prefers `secureCreds.syncCreds()` and falls back to `Settings.syncUsername/syncPassword` — so post-migration the ESP copy wins and pre-migration boots still work. Both `config()` and `resume()` go through it
- `DrawView.kt` - Touch handling, bitmap rendering, stroke/erase logic, and text-box place/edit/select/move/resize (Tool.Text); `composeStaticBitmap()` is the single layering point; `drawTextBox(canvas, box)` serves both the static bitmap and the live transform overlay; `measureTextBoxHeightPx(...)` (sibling of `drawTextBox`) recomputes rendered height for `commitOverlayBox`; `pendingNewBox` + `discardPendingNewBox` / `commitOverlayBox` drive the `TextBoxEditOverlay` round-trip; `onOverlayEditRequested` callback fires for both drag-to-draw (isNewBox=true) and Edit pill (isNewBox=false)
- `TextBoxEditOverlay.kt` - Full-screen edit modal (sibling of LibraryView/SettingsView/RecycleBinView). `show(host, box, isNewBox, fontCatalog, fontResolver, screenTextSize, focusForEditing, callbacks)` / `hide()` / `isShowing`. Inflates `view_textbox_edit.xml` and attaches to `android.R.id.content`. Owns local edit-time state (font/size/weight/border/zBand) and snapshots it into a `TextBox.copy(...)` on commit. `commitIfShowing()` / `requestCancel()` let foreign callers drive the same paths the buttons use
- `TextBoxEditOverlayLogic.kt` - Pure decisions: `commitDecision(rawText)` (trim → `Persist` or `DiscardEmpty`) and `cancelDecision(wasNewBox)` (`DiscardPendingNew` vs `NoOp`). 7 unit tests pin the contract
- `TextBoxMenuView.kt` - Floating Edit/Options/Delete pill for a selected box (mirrors SelectionMenuView; non-focusable so drag-to-move/resize touches reach the canvas)
- `FontCatalog.kt` - Off-thread `/system/fonts` enumeration; `resolve(name, weight)→Typeface` with system-default fallback for empty/absent fonts
- `ToolBar.kt` - UI toolbar with tool button state management (incl. Paste enable/armed state, Template action cell → per-page template picker, OCR action cell → opens the recognized-text viewer; greyed via `setOcrEnabled` when the active page has no live `page_text_from_server` row). The Fountain cell opens a **pen-settings popup** (A10): variant rows + a 5-chip width strip (chips drawn as thickness samples); tapping a variant/width updates the popup in place (no dismiss), tap-outside dismisses. Exposes `setOnPenWidthSelected`/`loadPenWidths`/`activePenWidthLevel`/`currentPenWidthLevels`/`setOnOcrClicked`/`setOcrEnabled`. The legacy Refresh cell was removed — the device's Quick Settings exposes a full-screen GC clear that does the same thing, and the toolbar's internal `fullRefresh()`/`gcRefresh()` calls fire automatically on page/notebook switch anyway
- `ToolSelectionLogic.kt` - Pure tool selection state machine (pen/erase variants + Lasso + **per-variant pen width level**, default M: `selectPenWidth`/`activePenWidth`/`penWidthFor`/`setPenWidthForVariant`/`allPenWidthLevels`)
- `PenWidthSettings.kt` - Pure bridge: `Settings.penWidthLevels` (`Map<String,String>`) ↔ `Map<PenVariant,PenWidthLevel>` (decode drops unknown names; consumer defaults to M)
- `LassoSelectionLogic.kt` - Pure selection geometry (ray-cast pointInPolygon, integer centroid, selectedIds, bounds, translate)
- `Clipboard.kt` - `Clipboard` interface + `InProcessClipboard` (listener-based; B1 re-backs it with `app_state.clipboard_json`)
- `SelectionMenuView.kt` - Floating action pill (PopupWindow) for a lasso selection; non-focusable so drag-to-move touches reach the canvas
- `SelectionActionLogic.kt` - Pure dialog-text rules. Recognize (F1) is now the *remote-override placeholder* — only called when `selectionRecognitionUrl` is non-empty (routed via `RecognizeFlowLogic.decide`); copy tells the user to clear the field to switch to on-device. To-do (F2) placeholder is no longer reached on the happy path — the real lasso → recognize → CalDAV VTODO flow now ships behind `secureCreds.caldavCreds()` (see Key Decisions and `caldav/*`)
- `RecognizeFlowLogic.kt` - Pure decision layer for the lasso-pill Recognize tap. `decide(strokeCount, url, modelPresent, langTag) → FallbackToPlaceholder | PromptDownload | ProceedToRecognize`; `describeResult(Result<RecognizedText>) → ResultUi.Show | Retry | Error`. Imperative orchestration in `MainActivity.showRecognizeFlow` stays thin
- `recognize/Recognizer.kt` - Interface contract for handwriting recognizers (`suspend recognize(strokes, langTag) → Result<RecognizedText>`) + `RecognizedText` + sealed `RecognizerError` (`ModelMissing`/`Empty`/`Unknown`). Pure-Kotlin; no Android imports — testable in JVM
- `recognize/StrokesToInk.kt` - Pure conversion `List<Stroke> → InkRequest/InkStroke/InkPoint`. Virtual coords pass through unchanged; pressure dropped (MLKit's Latin model ignores it); empty strokes filtered. Fully unit-tested. The MLKit-specific glue is one extension function in `MlKitInkAdapter.kt`
- `recognize/MlKitInkAdapter.kt` - One-line extension `InkRequest.toMlKitInk(): com.google.mlkit.vision.digitalink.Ink`. The *only* file in the module that imports MLKit's `Ink` type
- `recognize/RecognitionModelManager.kt` - Defensive wrapper around MLKit's `RemoteModelManager` (`isDownloaded` / `download` / `delete` / `installedLanguages`). Every call wrapped in try/catch; never throws. Holds the curated `SUPPORTED_LANGS` list + `displayName(langTag)` helper
- `recognize/MlKitRecognizer.kt` - Real `Recognizer` impl backed by `DigitalInkRecognizer`. Lazy per-language cache; `MlKitException.NOT_FOUND` is mapped to typed `RecognizerError.ModelMissing` so `RecognizeFlowLogic.describeResult` can loop back to download. `close()` releases the cached recognizers (called from `MainActivity.onDestroy`)
- `recognize/VlmRecognizer.kt` - Stub for a future tiny-VLM backend; satisfies the `Recognizer` interface but returns `RecognizerError.Unknown("VLM backend not implemented yet")`. Exists to keep the interface honest for the next backend swap
- `SettingsView.kt` - Full-screen Settings overlay (B2): inflated over the editor via `addContentView` onto `android.R.id.content`, reusing MainActivity's single `NotebookStore` (a 2nd Activity would open a 2nd DB connection and break single-writer serialization). Editor template+pitch radios persist on change; Sync/AI URL fields commit on blur or IME Done. `show(...)` signature now requires `secureCreds: SecureCredentialsStore` — sync username/password fields read from + write to ESP (post-migration source of truth), no longer Settings. CalDAV section ("Calendar (CalDAV tasks)") writes collection URL / username / password to ESP via a `wireSecure(...)` helper (blur or IME Done discipline). A "Test connection" button fires a Basic-auth `OPTIONS` request off-thread (`scope.launch + Dispatchers.IO`) and toasts the HTTP code. Includes a "Recognition models" section that lists installed MLKit Digital Ink languages with a Delete affordance and exposes a Download picker (uses the host `MainActivity.modelManager` passed in to `show(...)`). Reached via a Settings action in the Library; system back dismisses it (`MainActivity.onBackPressed`)
- `SettingsFormLogic.kt` - Pure pitch-radio rules (presets 5/7/10mm, visible iff template != BLANK, nearest-preset snap, index clamp)
- `TemplateGeometry.kt` - Pure template geometry (interior `lineOffsets(extent,pitch)`) + effective-config resolution (`page override ?: global default`, AC8.4)
- `res/layout/view_settings.xml` + `res/values/styles.xml` - Settings layout (header Back/title + scrolling sections) and its field styles (black-on-white, e-ink friendly)
- `res/layout/view_textbox_edit.xml` - Full-screen text-box edit overlay: header (Cancel | title | Done), weight-1 EditText body, bottom style strip (font Spinner / size RadioGroup / weight Toggle / border CheckBox / z-band RadioGroup) inside a HorizontalScrollView
- `PageNavigationLogic.kt` - Pure page navigation (index, prev/next bounds, "N / M" label, can-delete)
- `res/layout/toolbar.xml` - The eight tool cells: Fountain / Lasso / Text / Erase / Paste / Clear / OCR / Template (each whole cell is the hitbox). `Text ▾` opens the font/size chooser; OCR opens the recognized-text viewer (greyed when the active page has no server-OCR row yet)
- `ocr/OcrTextDialog.kt` + `res/layout/dialog_ocr_text.xml` + `res/drawable/ic_ocr.xml` - Editor's recognized-text viewer: a modal AlertDialog with a Spinner picking the source (Server / Device — device is a forward-compatible stub showing "not available yet"), a meta line (model · ocr_at), a scrollable selectable TextView, and a manual Refresh button. `show(context, store, pageId, onRefresh)` re-loads via `NotebookStore.loadPageTextFromServer(pageId)`; the open dialog also exposes `update(rt)` so the host can push fresh data in without dismissing. Renders dimmed (alpha 0.45) with an "OCR pending" badge when the row's `isStale` flag is set (local mutation since the last server upsert — see core:format Gotchas). `MainActivity.refreshOcrButtonState()` re-queries on page/notebook switch and on every `SyncStatus.Synced` so the toolbar gate reflects newly-arrived OCR rows AND auto-pushes fresh data into the open dialog (page-scoped callback guards prevent cross-page bleed); `MainActivity.refreshOcrInDialog()` is the dialog's manual-refresh callback
- `res/layout/activity_main.xml` - navbar + a `canvas_container` FrameLayout wrapping DrawView (so the text-box EditText can overlay the canvas)
- `res/layout/dialog_page_template.xml` - Per-page template picker (B4): "Use default" (snapshots current default) + Blank/Dot/Ruled/Grid + pitch sub-radio
- `res/layout/navbar.xml` - Unified top bar: notebook label / prev (◀) / "N / M" indicator / next (▶) / `<include>` of `toolbar.xml`; a 1dp divider in `activity_main.xml` separates it from the canvas. `toolbar.xml` (root id `@id/toolbar`) is included here rather than placed at the bottom.
- `res/layout/dialog_notebook_properties.xml` - Long-press → Notebook Properties (name + Created/Modified/Pages)
- `RecycleBinView.kt` + `res/layout/view_recycle_bin.xml` - Full-screen Recycle Bin overlay (E3): mirrors `SettingsView` (`show/hide/isShowing`, reuses the single `NotebookStore`), opened from the Library header's now-enabled Recycle cell (which shows a "Recycle (N)" count badge), system-back closes it (`MainActivity.onBackPressed`, before the Settings/Library checks). Rows are built programmatically into a ScrollView (not a RecyclerView) — each bin top (standalone notebook or folder batch) with Restore / Delete-forever; header has Empty Bin. Folder soft-delete is reachable via the Folder Properties **Delete** button (`MainActivity.confirmDeleteFolder` → `store.deleteFolder`). E4 retention: a "Auto-empty after N days" field in Settings (`Settings.recycleBinRetentionDays`), purged at launch in the repo's `bootstrap()`
- `LibraryView.kt` - Full-screen Library overlay (C3a/C4/C5): a 4-column RecyclerView grid of folders-then-notebooks in the current folder (`currentFolderId`, null = root). Like `SettingsView`, an overlay View (not an Activity) reusing the single `NotebookStore`. Tap a notebook → open; tap a folder → descend; breadcrumb/back → walk up; Search cell in the header (icon-only, no `▾` since it's a direct action) opens the search dialog. Header counts: a compact `[ic_notebook] N  [ic_folder] N` cluster sits inline beside the title segment of the breadcrumb (library-wide totals via `NotebookStore.libraryTotals` → `text_notebook_count`/`text_folder_count`; replaced the old "N notebooks across M folders total" line). Data via `NotebookStore`; folder/notebook dialogs + Settings + Search via `LibraryView.Callbacks` driven by `MainActivity`. Public `navigateToFolder(folderId?)` lets the search dialog jump the (already-showing) Library into a folder hit
- `search/SearchDialog.kt` + `search/SearchResultsAdapter.kt` + `res/layout/dialog_search.xml` + `res/layout/row_search_hit.xml` + `res/layout/row_search_truncated.xml` + `res/drawable/ic_search.xml` - Library search dialog: a modal `AlertDialog` with an `EditText` (IME `actionSearch`) + a `RecyclerView` of result rows (icon + title + bolded-match snippet via `SpannableString`/`StyleSpan(BOLD)`, ~56dp tall to fit a phone-sized dialog on the Mini). Adapter renders all four `SearchHit` kinds through one row layout plus an italic "truncated" footer when any branch capped at `NotebookRepository.SEARCH_DEFAULT_LIMIT`. Submit (magnifier button or keyboard IME) calls `NotebookStore.search`; stale post-dismiss callbacks are dropped via a `dialog` snapshot check. Tapping a result dismisses first, then routes via `SearchDialog.Callbacks` → `MainActivity` (`navigateToFolder` / `goToNotebook` / `goToNotebookPage`). `onPause` dismisses to avoid a window leak
- `LibraryAdapter.kt` + `LibraryItem.kt` - RecyclerView adapter over `sealed LibraryItem (Folder(FolderCard) | Notebook(NotebookCard))`; binds thumbnails via `ThumbnailLoader`/`ThumbnailCache`
- `LaunchLogic.kt` - Pure: `shouldOpenLibraryOnLaunch(activeNotebookId?, notebookCount)` — open the Library at launch only when there is no notebook to resume into (C6)
- `BreadcrumbLogic.kt` + `BreadcrumbView.kt` - Pure breadcrumb trail math + its top-bar View for the current folder path (C5)
- `NotebookNameParser.kt` - Pure parse of a notebook display name
- `RelativeTime.kt` - Pure "modified N ago" formatting for cards
- `ThumbnailRenderer.kt` / `ThumbnailCache.kt` / `ThumbnailCacheLogic.kt` / `ThumbnailLoader.kt` - Off-thread notebook-thumbnail render (first page's strokes) + LRU cache; `ThumbnailCacheLogic` holds the pure sizing/eviction math
- `res/layout/view_library.xml` + `res/layout/item_folder_card.xml` + `res/layout/item_notebook_card.xml` - Library grid + its folder/notebook card cells
- `caldav/VTodoBuilder.kt` - Pure iCalendar VCALENDAR/VTODO emitter. `VTodoInput(uid, summary, description?, due: VTodoDue?, created: Instant)` + sealed `VTodoDue` (`DateOnly(LocalDate)` / `UtcInstant(Instant)`). Hand-built — no dav4jvm/ical4j. Emits CRLF line endings, 75-octet UTF-8-boundary-aware line folding, and RFC 5545 §3.3.11 TEXT escaping (`\` `;` `,` `\n`). Blank DESCRIPTION omitted. 8 tests pin the golden body + edge cases
- `caldav/CalDavClient.kt` - `interface CalDavClient { suspend put(creds: CalDavCredentials, uid: String, body: String): CalDavResult }` + `CalDavCredentials(collectionUrl, username, password)` + sealed `CalDavResult` (`Ok` / `HttpError(code)` / `TransportError(cause)`). Production `OkHttpCalDavClient`: PUT to `{collectionUrl}{uid}.ics` (collectionUrl trailing-slash-normalized), `Content-Type: text/calendar; charset=utf-8`, `If-None-Match: *`, Basic auth. 2xx → Ok, 4xx/5xx → HttpError(code), any exception → TransportError. MockWebServer tests pin happy path + URL normalization + 401 + transport-failure
- `caldav/SecureCredentialsStore.kt` - Pure-ish store over a `KeyValueBackend` interface. `SyncCredentials(username, password)` + `CalDavCredentials` (from CalDavClient). `syncCreds()`/`setSyncCreds()`/`caldavCreds()`/`setCaldavCreds()` — getters return null unless ALL required fields are non-blank (sync: 2; caldav: 3). `migrateSyncCredsFromSettings(view: SettingsCredsView) → MigrationResult` (`Migrated` / `AlreadyDone` / `NothingToDo`) is the one-shot drainer called from `MainActivity.onCreate`; on `Migrated` the caller writes back an empty-string view via `store.updateSettings`. 9 unit tests pin the null-when-blank rule, round-trip, set-null clear, three-field gate, and the three migration paths
- `caldav/EncryptedPrefsCredentialsBackend.kt` - Production `KeyValueBackend` over `androidx.security.crypto.EncryptedSharedPreferences` (file `forestnote_secrets`, AES-256 key in Android Keystore). Defensive — every put/get wrapped in try/catch; on Keystore/IO failure it degrades to "no creds" (returns null / silently drops writes) rather than crashing the app
- `caldav/CalDavTaskSheetLogic.kt` - Pure: `DueChoice` enum (None / Today / Tomorrow / NextWeek / Pick) + `resolveDue(choice, zone, today, picked) → VTodoDue?` (Pick wins, fixed offsets otherwise, None → null), `validateSummary(text) → Boolean` (non-blank), `pillLabel(text, maxLen)` ellipsizer for the floating pill. 9 unit tests
- `caldav/CalDavTaskSheet.kt` - Full-screen modal sheet (peer of `SettingsView`/`TextBoxEditOverlay`): SUMMARY EditText, DUE chip row (None / Today / Tomorrow / +1 week / Pick…), optional NOTE field, Cancel/Send footer. `show(host, prefill, callbacks)` / `requestCancel()` / `isShowing`. On Send: validates via `CalDavTaskSheetLogic.validateSummary`, resolves the due via `resolveDue`, builds the `VTodoInput`, and fires `Callbacks.onSend` (host wires this to `NotebookStore.enqueueCalDavTask` + `CalDavOutboxDrainer.tryImmediately`)
- `res/layout/view_caldav_task_sheet.xml` + `res/drawable/chip_caldav_due_selected.xml` + `res/drawable/chip_caldav_due_unselected.xml` + `CalDavDueChip` style - Layout + drawables + chip style for the CalDAV task sheet (the previous intermediate `view_recognize_pill.xml` was removed — `CalDavTaskSheet` is reached directly from the lasso pill's To-do button)

## Gotchas
- ViwoodsBackend cast in onResume: only safe because isEInk guards it
- Crash handler writes to /sdcard/Download/ first, falls back to app filesDir
- E-ink mode disables animations and ripple effects to prevent ghosting
- **Never call `drawView.gcRefresh()` while a non-editor View is on screen** (any dialog or full-screen overlay). The Viwoods writing overlay composites ABOVE the regular View pipeline on this panel, so `gcRefresh`'s `pushBackgroundBitmap` puts the editor bitmap on top of whatever else is showing. Safe contexts: editor↔editor transitions (page/notebook switch — funnel through `refreshPageIndicator`'s callback) and dialog/overlay DISMISS paths. `OcrTextDialog.show` documents this rule inline; learned the hard way building task #12
