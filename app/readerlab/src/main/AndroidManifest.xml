<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application android:label="ForestNote Reader Lab" android:theme="@style/ReaderLabTheme"
        android:allowBackup="false" android:usesCleartextTraffic="false" tools:replace="android:label,android:allowBackup">
        <activity android:name=".ReaderLabActivity" android:exported="true" android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
